# 更新日志

> 本文件记录各版本变更草稿，内容最终会合并进 `README.md` 的「版本历史」章节。
> 保留在仓库中以备追溯；README 面向用户，本文件面向开发。

## V2.2.0 -- 当前版本（2026-09-18）

本版是一次以「界面重构 + 大量缺陷修复」为主的更新，共 20 次代码变更。

### 新增
- **软件著作权登记**：本项目已取得《计算机软件著作权登记证书》。
  「设置 → 关于我们」新增「软件著作权」卡片，展示登记号、证书号、著作权人、
  登记日期与权利范围，并提供「查看登记证书」按钮，可缩放查看证书扫描件。
  证书原件同时收录于仓库 `copyright/` 目录。
- **顶部栏实时状态**：标题栏新增 UI 帧率与网络上行/下行实时显示
  （矢量图标 + 数值，无背景），可直接观察界面是否卡顿、当前下载速度。
- **顶部实时状态的网络统计**：基于 `psutil.net_io_counters()` 前后差值计算，
  帧率按事件循环 tick 统计，每秒刷新一次。
- **下载设置新增「重新下载前重试次数」等既有项**：沿用上一版，本版无变更。

### 界面重构（大气、扁平、自适应）
- **引入设计令牌体系**：`_BASE_STYLE` 重写为基于设计令牌的全局样式，
  统一色板、圆角、间距、状态（悬停/按下/禁用/聚焦），并统一了 138 处硬编码色值。
- **全面改为直角**：449 处 `border-radius` 拉平为直角（仅保留头像、
  单选钮、状态点等真正的圆形元素），整体更大气。
- **字号体系统一为 4 档**：此前实际渲染出 12 种不同字号（13~51px），
  显得有大有小；现收敛为 22/25/27/28px 四档，观感统一。
- **窗口自适应重做**：
  - 最小高度由 1159px 降到 772px，768P 笔记本可用；
  - 内容超长时整体滚动，不再出现控件重叠；
  - 修复超宽屏下内容被拉散、字段散落两端的问题；
  - 修复「无论怎么调都出现左右滚动条」——根因是标题栏自身最小宽度
    被超长帮助按钮与状态行顶到 1438px，现降到 754px；
  - 修复「拖动标题栏就直接最大化」——启动守卫只在边缘缩放时解除，
    拖动标题栏未解除，现已在拖动时同样解除，并让最大化状态拖动先还原。
- **设置窗口重做**：侧边栏与卡片拉平为直角；修复三处控件重叠
  （程序图标卡、系统托盘、合集文件夹下拉）；设置内文字不再被写死成 12px。
- **按钮层次重做**：由 7 种撞色收敛为「主操作实心蓝 / 次操作浅底描边 /
  危险红」三级；修复按钮文字被上下挤压（11 处最小高度小于字体高度）。
- **下拉菜单补齐箭头指示**：此前全局样式去掉了默认箭头又未提供替代，
  下拉框与普通输入框无法区分；现补上矢量箭头，并美化展开列表
  （条目高度、悬停、选中态）。
- **未激活标签可见**：此前未选中标签背景全透明，与页面底色无差别，
  看不出是标签；现改为浅灰底 + 边框。
- **弹幕解析页填充**：此前只有一个信息分组、未解析时整片空白；
  新增「弹幕预览」列表与空状态提示，解析后逐条列出弹幕内容。
- **系统信息卡收紧**：四条诊断信息由两行改为一行四列，
  卡片高度 209 → 184px。
- **视频解析页重构**：把「视频信息 + 清晰度/音质/选择集数 + 完全模式 +
  保存路径」收成一张自洽的卡片，消除卡片内部与下方的空白。
- **选择集数对话框**：
  - 卡片模式此前右侧空出一整张卡片的宽度（网格宽度未扣首列内边距，
    Qt 因此少排一列），现已铺满；
  - 卡片模式此前必须手动拖动窗口才正常（切换视图时未重算网格，
    而卡片是按陈旧 gridSize 创建的），现首次切换即正确；
  - 列表模式封面此前失败不重试，现按 1.2s/2.4s/3.6s 退避重试最多 3 次；
  - 列表模式封面下载期间不再误显示「无封面」，改为「加载中…」；
  - 修复并发加载计数器变成负数的问题。

### 修复
- **同一视频被反复下载**：日志实证同一次提交在 11 秒内创建 50 个任务。
  已在 UI 两个下载入口加 3 秒防重入节流，并在 `DownloadManager.start_download`
  增加基于「url + qn + 集数 + 保存目录」的 5 秒短时重复提交拦截。
- **停止解析会清空结果**：`parse_media` 的 4 处取消检查点全都
  `return {"success": False}`，把已解析到的集数全部丢弃。
  现改为返回已解析到的部分结果（`success=True` + `cancelled/partial`），
  界面提示「解析已停止，已保留 N 个已解析结果」。
- **登录界面输入框样式错乱**：6 个输入框的内联样式只写 `font-size + padding`，
  在 Qt 中会全量替换控件规则，导致全局的边框/圆角/背景全部失效。
- **主窗口有时比屏幕还大**：恢复上次窗口几何时只夹紧位置未夹紧尺寸，
  若上次几何来自更大的显示器就会超出屏幕。
- **设置里「程序图标」只有默认能显示**：备用图标路径未包含 `assets/icons`，
  导致「新奇」「自定义」显示为空白文件图标。
- **任务栏图标空白**：`get_app_icon()` 在解析失败时返回空 QIcon，已加多级兜底。
- **仅下载音频不带文件名**：产出形如 `第1集_v27-daily-a.mp3`，
  因 `ep_info` 无 `ep_title` 时未回落到视频标题。
- **下载失败任务重试**：核对现有逻辑（`max_retry` 默认 3，重试会重新解析播放地址）
  并补强健壮性——卡片模式 `episode_num` 为整数时 QLabel 抛 TypeError
  会导致选择对话框打不开。
- **选择集数卡片模式首次打开封面为「无封面」**：见上「界面重构」相关条目。

### 打包
- 软著证书图片随程序分发（`build/build.py` 打包 `assets/copyright`）。
- 证书查看图改用 300dpi 高分辨率（2480×3355），放大后文字清晰可读。

### 补充修复（同一版本追加）
本轮为打包前集中的缺陷排查，全部经日志实证或本机复现：

- **Cookie 过期被误判为「已登录」，导致拿不到 1080P**
  原判定只看 Cookie 里**是否存在 SESSDATA 这个键**，而 SESSDATA 过期后键依然存在，
  于是被判成已登录 → `try_look=0`；未登录本应 `try_look=1`
  （官方文档：`try_look=1` 可让未登录拿到 720P/1080P）。
  结果 Cookie 过期的用户反而只能拿到 360P/240P。
  现改用 nav 接口的**实际登录状态**判断；nav 异常时才退回宽松判断。
  实测（同一过期 Cookie）：可用清晰度 `[1080P, 720P, 480P, 360P]`（修复前仅 360P/240P）。

- **视频工具「开始转换」后误判失败**
  ffmpeg 实际已完整处理完视频（输出文件已生成），却报「返回码: None」。
  原因是读到 stderr EOF 即跳出循环，此时子进程尚未被收割，`returncode` 仍为 `None`，
  而 `None != 0` 为真被判为失败。现先 `wait()` 收回退出码再判定。

- **视频工具卡死/闪退**
  上述误判会进入异常收尾路径，使主线程长时间阻塞，触发内置看门狗
  「主线程 12 秒未响应」后注入异常并重启进程。
  另修一处潜在死锁：ffmpeg 的 stdout 用 PIPE 却从未读取，
  写满 64KB 缓冲即阻塞进程，现改为丢弃。

- **视频工具报「未找到 ffmpeg」，随后卡死闪退**
  `_build_ffmpeg_cmd` 漏把解析到的 ffmpeg 路径放进命令（`cmd[0]` 成了 `-y`），
  Popen 必然抛 `FileNotFoundError`；且该分支在**转换子线程**里弹 `QMessageBox`
  （跨线程模态窗口会卡死/闪退）。现恢复路径写入，并改为只写日志不弹窗。

- **视频工具找不到 ffprobe**
  解析 ffprobe 时得到的是**裸文件名**，只去系统 PATH 找，
  而随程序分发的 ffprobe 在 `ffmpeg/bin/` 下且未加入 PATH。
  现统一按「配置 → tool_manager → 随程序分发目录 → PATH」四级解析。

- **下载历史「用视频工具处理」提示文件不存在**
  `add_record` 本就支持 `save_path`/`file_path`，但四处调用方都没传，
  历史记录里两个路径字段永远为空。现下载成功即记录真实产物路径。

- **番剧页的额外合集（元祖迷你动画 / 专访 / PV）无法解析**
  额外合集在 `result["section"]`（单数），原代码只查了 `result["sections"]`（复数）；
  且当前 API 的正片在 `result["episodes"]`，原代码只查 `main_section`，
  会出现「只有迷你动画、正片全丢」。现全部补齐，并按合集分组展示。

- **番剧试看接口未覆盖「已登录」用户**
  `try_look` 仅在未登录时启用，已登录的普通用户打开大会员专享集会直接取流失败；
  另有 11 处备用取流链接完全没有 `try_look`。现均已补齐。

- **保存路径选择窗口**
  去除全部 emoji、统一改用项目图标库，并把原先挤在一行的
  「返回/前进/主页/桌面/下载/视图/搜索」拆成导航行与操作行。

- **新增「启动时最大化窗口」设置**（设置 → 窗口设置，默认开启）。
  此前启动流程有三处无条件最大化（其中 `hide_cookie_ui` 在隐藏界面时也调用
  `showMaximized()`），会导致该设置完全失效。

- **在收藏夹只选 2 个视频却下载了整个合集**
  下载入口的兜底逻辑没有模式判断，只要未显式选集数就把整个合集全选。
  现改为默认只下载当前解析的视频。

- **「继续」按钮会重新触发整个任务**
  该按钮在 paused/failed/pending 三种状态下都出现，但只有 paused 走续传，
  其余状态会新建任务并把全部集数重下。现只要任务仍在内存即走续传（跳过已完成集）。

## V2.1.11（内测）

### 新增
- **文件名添加UP主名**：下载设置新增「文件名添加UP主名」，勾选后文件名以 `[UP主] ` 开头，多P/合集下载更易区分
- **UP主 / 合集文件夹**：下载设置新增「保存到UP主文件夹」与「创建合集文件夹」，合集文件夹位置可选（UP主文件夹内 / 独立），多P视频可自动归档
- **收藏夹刷新方式**：新增「收藏夹刷新」选项，可选择手动或自动（默认手动），不再一切到收藏夹标签就自动拉取

### 修复
- **MSIX 版日志/设置被拒绝访问**：微软商店版安装目录只读，日志、设置、设备标识统一改存到用户可写目录，复制/打包日志不再报"拒绝访问"
- **合集/分P解析只剩第1P**：修复带 `p=` 的合集链接解析时被"指定分P过滤"误缩成单P，现可完整解析全部P
- **登录闪退**：修复 curl_cffi Session 非线程安全导致的扫码/取码并发崩溃
